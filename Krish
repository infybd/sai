BusiestDay = 
CALCULATE(
  MAX('Table'[Date]),
  FILTER(VALUES('Table'[Date]), 
  CALCULATE(COUNT('Table'[ExceptionType])) = 
  CALCULATE(MAXX(ALL('Table'[Date]), COUNTROWS(FILTER('Table', 'Table'[Date] = EARLIER('Table'[Date]))))))
)
